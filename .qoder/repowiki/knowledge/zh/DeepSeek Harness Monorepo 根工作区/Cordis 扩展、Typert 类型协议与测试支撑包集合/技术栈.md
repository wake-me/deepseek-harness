TypeScript ESM (`"type": "module"`)；构建使用 tsdown（`tsdown.config.ts`）；Cordis 插件框架（`@deepseek-ai/cordis`）；React 18 用于 UI 扩展；Vitest 用于单元测试与快照；`@agentclientprotocol/sdk` 用于 ACP 协议；`typescript` 编译器 API 被 typert/generator 用于静态分析。