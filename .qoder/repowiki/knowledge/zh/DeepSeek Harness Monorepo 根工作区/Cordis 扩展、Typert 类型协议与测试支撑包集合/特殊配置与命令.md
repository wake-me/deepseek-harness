各子包通过 workspace 协议相互引用（如 `workspace:^`），需在工作区根执行 pnpm 安装；`dsh` 字段声明了 Cordis 插件的注入依赖与平台（如 `platform: "web"`）；部分包提供 `bundle`/`watch` 脚本调用 tsdown，`acp-snapshot` 仅在 vitest 环境中可导入（其 `suite.ts` 直接依赖 vitest）。