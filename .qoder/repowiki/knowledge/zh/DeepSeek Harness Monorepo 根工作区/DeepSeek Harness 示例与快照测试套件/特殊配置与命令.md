运行单个示例需进入对应子目录执行 `dsh run`（或对应 bin），并在根 `.env` 提供 `DEEPSEEK_API_KEY`；录制快照用 `DSH_SNAPSHOT=record dsh run ...`，回放用 `DSH_SNAPSHOT=1` 驱动 `*.snapshot.ts`；ACP 真实 e2e 需要 `DEEPSEEK_API_KEY` 且默认跳过（`describe.skipIf(!process.env.DEEPSEEK_API_KEY)`）。