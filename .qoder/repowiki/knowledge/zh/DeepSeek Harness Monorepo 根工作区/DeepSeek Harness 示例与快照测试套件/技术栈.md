Node.js ESM (`"type": "module"`) + Vitest 测试框架；Cordis 插件系统（YAML 声明式 `cordis.yml` 装配 `@deepseek-ai/dsh-*` 插件）；ACP 协议基于 `@agentclientprotocol/sdk`；快照回放使用 JSONL 日志与 zstd 压缩；部分场景通过 `DSH_SNAPSHOT` 切换为纯回放模式。