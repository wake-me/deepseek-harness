- 每个 packages/*/<domain>/<feature> 子包遵循统一的 types/client/invariant 导出结构，并在 tsconfig.base.json 中以 @deepseek-ai/dsh-*/types|client|invariant 别名注册。
- 跨子包通信与装配统一基于 Cordis 插件模型，由 apps_cli 在启动时通过 cordis.yml 声明式组装。
- 测试按场景拆分 vitest.*.config.ts 并复用 vitest.shared.ts 中的标准装饰器预处理与 webstorage 标志。
- 仓库级质量门禁通过 scripts/run-gates.ts 的 check-* 子命令统一编排，而非在各子包各自执行 lint/test。
- vendor 上游框架通过 pnpm overrides/linkWorkspacePackages 强制锁定到本地版本，禁止子包引入不同版本的 cosmokit/schemastery。