`python/sdk/pyproject.toml` 中通过 `[tool.uv.sources]` 将 `deepseek-harness-runtime-bin` 指向 `../sdk-runtime` 并以 editable 模式安装，使 SDK 在开发环境直接消费同仓构建出的运行时二进制；测试通过 pytest 运行（`pytest -q`），位于 `python/sdk/tests/`。