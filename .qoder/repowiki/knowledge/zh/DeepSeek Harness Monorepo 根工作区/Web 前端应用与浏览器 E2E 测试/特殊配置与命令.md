不可直接用 `vite serve` 启动——`rejectStandaloneServe` 插件会在 `serve` 模式下抛错，必须通过 `pnpm dsh web`（仓库内）或安装后的 `dsh web` 命令启动；开发时 HMR 需同时运行 `pnpm dsh web` 与 `pnpm run dev:web`。E2E 测试通过 `DSH_SNAPSHOT=replay|record|refresh` 控制回放/录制/刷新黄金文件模式。