worker 入口同时支持开发态直接加载 `src/worker.ts`（带 `.ts` 后缀时）与发布态加载 `lib/worker.cjs`（CommonJS 单文件 bundle），由 `WORKER_PATH` 根据 `import.meta.url` 路径自动选择；测试覆盖包含 `bootstrap.spec.ts`、`runtime.spec.ts`、`source-worker.compat.spec.ts` 等，需以 Vitest 运行。