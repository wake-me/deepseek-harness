Node.js `worker_threads` 隔离执行；TypeScript 源码通过 `node:module.stripTypeScriptTypes` 在线剥离类型；配置校验使用 `@deepseek-ai/schemastery`；JSON 序列化边界由 `@deepseek-ai/dsh-session.snapshotJsonValue` 保证；超时上限受 `@deepseek-ai/dsh-timeout.MAX_TIMER_DELAY_MS` 约束。