TypeScript + Node.js；进程隔离后端使用 bubblewrap（bwrap）、Linux Landlock（通过 `@deepseek-ai/node-addon-landlock-run`）、macOS sandbox-exec（Seatbelt）、Windows WRITE_RESTRICTED 受限令牌 + ACL DACL 修改；配置校验使用 `@deepseek-ai/schemastery`；服务框架为 `@deepseek-ai/cordis`；Windows ABI 探针用 C++（`verify/abi-probe.cpp`）。