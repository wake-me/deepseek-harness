Windows 包包含 `verify/abi-probe.cpp`，需在构建前编译以验证 Win32 ABI 兼容性；`sandbox-local` 支持通过 `internals.platform` / `internals.chain` / `internals.probeBwrap` / `internals.probeLandlock` / `internals.probeSeatbelt` / `internals.windowsAclRunnerArgs` 等测试钩子在任意主机上模拟任意平台链；e2e 测试（`bwrap.e2e.ts`、`landlock.e2e.ts`、`seatbelt.e2e.ts`、`packed-install.e2e.ts`）需要宿主机实际安装对应运行时。