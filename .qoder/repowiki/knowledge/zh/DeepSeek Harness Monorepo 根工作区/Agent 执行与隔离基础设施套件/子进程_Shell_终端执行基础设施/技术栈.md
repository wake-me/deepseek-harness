TypeScript + Cordis 插件框架；子进程通过 Node `child_process` 与 `node-pty` 管理进程树与 PTY；PowerShell 执行器使用 `-NoLogo -NoProfile -NonInteractive -Command` 启动；bash 沙箱使用 bubblewrap (`bwrap`)、Linux landlock 与 macOS Seatbelt；配置校验统一使用 `@deepseek-ai/schemastery`。