本地构建需 Linux + `musl-tools`（提供 `musl-gcc`），执行 `pnpm run build:native`；跨架构产物由 CI 各 runner 分别产出后通过 `pnpm run release:assemble-prebuilds` 汇总，再经 `release:pack` / `release:verify` / `release:publish` 完成发布流程。非 Linux 主机上 `build:native` 会直接失败。