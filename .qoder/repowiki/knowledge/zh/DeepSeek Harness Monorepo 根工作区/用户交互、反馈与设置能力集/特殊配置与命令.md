各子包均为独立 npm package，拥有自己的 `package.json`、`tsconfig.json`、`tests/` 与多语言 README（`README.md`/`README.zh.md`/`README.i18n.yaml`），构建与测试遵循仓库根 monorepo 约定；`user-approval` 与 `settings` 额外包含 `tsdown.config.ts` 用于单独打包。