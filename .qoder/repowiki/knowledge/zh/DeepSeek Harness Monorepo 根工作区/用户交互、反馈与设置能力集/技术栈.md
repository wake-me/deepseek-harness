Cordis 服务模型 + `@deepseek-ai/schemastery` 运行时 schema 校验；`@deepseek-ai/dsh-typert-protocol` 的 `TypertRemoteService` + `@Remote` 装饰器暴露 RPC；`@deepseek-ai/dsh-scope` 的作用域层与 parent binding 用于命令与作用域隔离；`@deepseek-ai/dsh-storage-domain` 的 KvTable 承载消息反馈持久化。