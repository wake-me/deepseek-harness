- 每个子包遵循相同的目录结构：src 下按功能拆分文件并统一导出 index.ts，tests 下对应 *.spec.ts 单元测试
- 每个子包 src 目录下均包含 invariant.ts 用于运行期断言，并在测试中配套 invariant.spec.ts 覆盖
- 子包通过 Cordis Service 接口暴露能力，并以 package.json 声明依赖关系形成 scope → session/tools → agent-loop → agent 的装配层级
- README 同时提供英文与中文版本（README.md 与 README.zh.md）以及国际化元数据 README.i18n.yaml