`AgentLoop` 在构造函数中自动读取 `Config.agents` 数组，对每个条目根据是否提供 `resumeSessionId` 决定走 `restoreOrCreateConfigured`（先尝试从 `sessionPersistence` 恢复，不存在则 fallback 创建）或延迟 `resumeWith`；可通过 `CONFIGURED_AGENT_IDENTITIES_KEY` 在 Loader 挂载前覆盖配置的 sessionId/resumeSessionId。