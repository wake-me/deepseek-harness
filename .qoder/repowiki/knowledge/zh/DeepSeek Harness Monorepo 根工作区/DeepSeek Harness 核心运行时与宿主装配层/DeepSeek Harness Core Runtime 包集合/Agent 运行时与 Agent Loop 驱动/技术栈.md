基于 `@deepseek-ai/cordis` 的 Service/Fiber/Effect 框架管理生命周期与依赖注入；使用 Node `AsyncLocalStorage` 维护 process-local initiator 作用域；通过 `@deepseek-ai/dsh-session` 的 `SessionPreparation` 做事务性 session 准备；配置校验与运行时设置使用 `@deepseek-ai/schemastery` 与 `@deepseek-ai/dsh-settings`；工具调度通过 `@deepseek-ai/dsh-tools` 的 `TOOL_RUNTIME_SCHEDULER` 钩子。