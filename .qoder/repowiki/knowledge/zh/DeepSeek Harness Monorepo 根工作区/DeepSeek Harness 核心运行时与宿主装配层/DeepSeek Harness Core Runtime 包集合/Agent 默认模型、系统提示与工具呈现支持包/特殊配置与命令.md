每个子包均为独立 npm package，通过各自的 `package.json` 发布为 `@deepseek-ai/dsh-agent-default-model`、`@deepseek-ai/dsh-system-prompt`、`@deepseek-ai/dsh-agent-tool-presentation`；构建入口由各自 `tsconfig.json` 指定，测试位于 `tests/*.spec.ts`。