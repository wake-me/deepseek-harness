- 每个子包遵循相同的目录结构：src/index.ts 作为唯一入口，配套 invariant.ts 用于运行时断言，tests/ 下 spec 文件与源码一一对应
- 跨子包通信通过显式导出的 TypeScript 类型与函数契约完成，不共享内部实现细节
- Host 侧能力以插件形式提供，每个能力一个独立子包并通过 Cordis Loader 注册，apiproxy 用 *.schema.ts 与 *.ts 配对定义 RPC 接口
- 所有对外 API 使用 JSDoc 标注并由 apiproxy 自动生成 schema，保证前后端契约一致