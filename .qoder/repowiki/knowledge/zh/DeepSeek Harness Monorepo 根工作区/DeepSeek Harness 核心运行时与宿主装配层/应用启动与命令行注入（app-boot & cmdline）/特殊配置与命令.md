app-boot 使用 tsdown 构建（`tsdown.config.ts`），cmdline 使用标准 TypeScript 编译；两者均通过 `package.json` 的 `exports` 字段同时发布主入口与 `/invariant` 子路径，且 `files` 仅包含 `lib/index.js`、`lib/invariant.js` 与 `lib/types/**/*.d.ts`。