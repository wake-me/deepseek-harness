构建产物包含由 Typert 生成的 `lib/typert.host.js` 与 `lib/typert.remote-client.js`（见 `package.json` 的 `files` 与 `exports`），需在宿主环境中先安装 `@deepseek-ai/dsh-typert-protocol` 才能运行；测试位于 `tests/` 下，使用 `.spec.ts` 命名约定配合项目内测试框架执行。