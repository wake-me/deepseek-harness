- 每个子包遵循统一的三文件结构：index.ts 导出能力、invariant.ts 集中存放运行时断言、tests/ 下对应 spec 文件
- 插件对外仅暴露最小化 API 入口，内部实现细节不泄漏至宿主或其他插件
- 跨进程通信一律基于 Zod 定义的 schema 进行参数校验，schema 与 handler 成对出现
- 目录选择器族通过 abstract seam + 自动解析（probe/resolve）在启动时选择具体后端实现