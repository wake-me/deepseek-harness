- 每个子包 src/index.ts 仅导出对外 API，并在包内维护独立的 invariant.ts 用于运行时断言。
- Provider 与 Tool 包通过读取 ctx.subagents 或 ctx.tools 进行装配，不直接引用其他子包的实现。
- 所有子包遵循相同的 README 结构（README.md、README.zh.md、README.i18n.yaml）。
- 测试覆盖 spec 与 e2e 两类文件，并按包隔离 fixtures 与 mock server。