每个包均为独立的 npm workspace 包，需通过各自 `package.json` 的 `peerDependencies` 引入 `@deepseek-ai/dsh-subagent`、`@deepseek-ai/dsh-tools`、`@deepseek-ai/cordis` 等运行时依赖；`tool-subagent` 在启用 `backgroundMode: 'continuable'` 时要求对应 provider 具备 `prepareContinuable` 能力，否则挂载时报错。