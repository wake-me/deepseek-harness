基于 Cordis 插件体系（`defineTool` + `ctx.tools.register` + `ctx.on` 事件）与 Schemastery 做配置校验；通过 `@deepseek-ai/dsh-subagent` 的 `SubagentProvider` 抽象对接不同后端（spawn/acp/in-process），并通过 `ctx.subagents.startContinuable` / `followup` / `interrupt` / `registerContinuableSetup` 等扩展点实现可继续子代理生命周期。