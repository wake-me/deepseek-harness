TypeScript ESM 包；配置校验使用 `@deepseek-ai/schemastery`（zod 包装）；ACP 通过 `@agentclientprotocol/sdk` 0.25.1 驱动；Claude Code 通过官方 `@anthropic-ai/claude-agent-sdk` 并以自定义 `SpawnedProcess` 接入共享进程管理；Codex 通过 `JsonRpcLineTransport` 自建 app-server 0.147.0 协议适配；所有进程生命周期由 `@deepseek-ai/dsh-subprocess` 统一管理。