TypeScript + tsdown 构建；各子包均依赖 harness 的 Loader/Context/Tool/Command 抽象，并通过 `runtime-diagnostics/invariants` 做运行时不变量校验；workflow 使用独立 worker-thread 隔离执行，subagent 支持 in-process/fork/spawn/ACP/Claude Code/Codex/Dsh SDK 多种 Provider 适配器。