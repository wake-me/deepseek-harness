包通过 `package.json` 的 `exports` 暴露多个入口：`.`（主控制器）、`./invariant`（不变量 companion）、`./types`（host 类型）、`./client`（client 类型别名）。宿主需将 `PlanModeController` 作为 Cordis service 注入到 `ctx.planMode`，并可选注册 `commands` 与 `sessionProjections` 子上下文以启用 `/plan` 命令与投影视图。