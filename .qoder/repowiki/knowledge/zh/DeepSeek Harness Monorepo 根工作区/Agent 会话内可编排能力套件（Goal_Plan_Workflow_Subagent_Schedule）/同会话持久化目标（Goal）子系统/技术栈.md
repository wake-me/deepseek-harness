TypeScript ESM；事件溯源基于 `@deepseek-ai/dsh-session` 的 `session/events` 追加流；远程服务通过 `@deepseek-ai/dsh-typert-protocol` 的 `TypertRemoteService` + `@Remote` 装饰器暴露；配置与输入校验统一使用 `@deepseek-ai/schemastery`（zod wrapper）；构建产物由 `tsdown.config.ts` 生成。