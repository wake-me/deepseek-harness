各子包均为独立可发布 npm 包（`packages/goal/{goal,tool-goal,command-goal,goal-round-driver}/package.json`），需通过 workspace 依赖安装；`goal/` 使用 `tsdown.config.ts` 构建多入口；测试位于各包 `tests/` 目录，分别包含 `*.spec.ts` 与 e2e 用例（如 `goal.e2e.ts`）。