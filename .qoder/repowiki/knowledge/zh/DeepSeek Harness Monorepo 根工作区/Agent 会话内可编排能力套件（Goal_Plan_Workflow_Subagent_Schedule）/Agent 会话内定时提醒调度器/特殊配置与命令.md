作为 workspace 内的 npm 包发布，构建产物位于 `lib/`，通过 `package.json` 的 `exports` 同时暴露根入口与 `./invariant`、`./src/*` 原始路径；测试位于 `tests/` 下对应 `.spec.ts` 文件，需在工作区环境中安装 peerDependencies（`dsh-agent`、`dsh-session`、`dsh-tools` 等）。