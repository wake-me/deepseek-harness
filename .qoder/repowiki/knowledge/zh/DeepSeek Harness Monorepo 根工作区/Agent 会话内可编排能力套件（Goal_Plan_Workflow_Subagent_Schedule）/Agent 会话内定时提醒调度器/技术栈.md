TypeScript ESM 包 `@deepseek-ai/dsh-schedule`，基于 Cordis 上下文与 Agent 生命周期；依赖 `@deepseek-ai/dsh-session` 的事件流、`@deepseek-ai/dsh-tools` 的工具注册、`@deepseek-ai/dsh-llm` 的消息构造；使用 Node `setTimeout`（上限 `MAX_TIMER_DELAY_MS = 2_147_483_647`）与 `Intl.DateTimeFormat` 处理 IANA 时区。