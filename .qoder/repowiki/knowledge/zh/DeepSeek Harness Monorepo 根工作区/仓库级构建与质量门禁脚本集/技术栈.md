Node.js + TypeScript（直接 import `.ts` 源文件，由 pnpm/Vitest 驱动运行）；大量使用 `typescript` 编译器 API 做 AST/Program 分析；Markdown 处理走 `mdast-util-*` + `micromark-extension-gfm`；并发调度基于 `node:child_process.spawn` 与 `availableParallelism`。