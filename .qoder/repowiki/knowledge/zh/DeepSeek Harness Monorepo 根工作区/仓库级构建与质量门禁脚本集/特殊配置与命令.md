入口统一为 `pnpm run <mode>`，由 `run-gates.ts` 解析 mode 并调度子进程；可通过 `DSH_GATE_CONCURRENCY` 控制全局并发、`DSH_COVERAGE_MAX_WORKERS` 拆分覆盖率工作线程、`DSH_OXLINT_THREADS` 调整 lint 线程数、`DSH_DOC_TYPECHECK_USE_BUILD_OUTPUT=1` 切换 doc-typecheck 使用已构建产物。Windows 下需通过 `wine-windows-gates.sh` 或 `prepare-ci-bubblewrap.sh` 准备环境。