TypeScript + Cordis 插件体系；JSON 后端使用 Node `fs/promises` 原子重写；SQLite 后端使用 `better-sqlite3`；附件存储使用 `crypto.createHash('sha256')` 内容寻址 + POSIX `link` 原子发布；凭据文件使用 `yaml` 解析与 `chokidar` 监听；配置校验统一使用 `@deepseek-ai/schemastery`。