每个子包均为独立 npm package（见各自 `package.json`），通过 workspace 协议声明 peer/dev 依赖（如 `@deepseek-ai/cordis`、`@deepseek-ai/dsh-scope`、`@deepseek-ai/dsh-llm`）；运行时通过环境变量 `DSH_HOME`、`DSH_AGENTS_HOME`、`DSH_BUNDLED_SKILL_DIR` 控制 skill 根目录与 bundled 目录。