包以 ESM 发布（`type: module`，`main: lib/index.js`，`types: lib/types/index.d.ts`），需通过 workspace 内 `@deepseek-ai/dsh-*` peerDependencies 解析；在 `cordis.yml` 中以插件实例方式声明，每个 MCP 服务器一个实例，`serverName` 必须全局唯一且匹配 `/^[A-Za-z-_-]{1,32}$/`。