基于 `@modelcontextprotocol/sdk` v1.12+ 的 MCP Client；配置与运行时校验使用 `@deepseek-ai/schemastery`（Zod 包装）；进程环境清洗来自 `@deepseek-ai/dsh-subprocess`；超时上限来自 `@deepseek-ai/dsh-timeout`；工具注册与执行契约来自 `@deepseek-ai/dsh-tools`；宿主框架为 `@deepseek-ai/cordis`。