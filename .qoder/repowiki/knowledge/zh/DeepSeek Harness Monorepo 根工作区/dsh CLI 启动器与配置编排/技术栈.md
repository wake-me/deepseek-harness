TypeScript + ESM；CLI 参数解析使用 commander v15；打包使用 tsdown（`tsdown.config.ts`）；依赖大量 workspace 内的 `@deepseek-ai/dsh-*` 包（app-boot、cmdline、headless、terminal、mcp-client、skill、tool-* 等）以及 `@deepseek-ai/cordis` 插件框架。