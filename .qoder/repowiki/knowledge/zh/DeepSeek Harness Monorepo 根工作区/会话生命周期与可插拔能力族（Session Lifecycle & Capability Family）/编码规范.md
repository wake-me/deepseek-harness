- 每个子包同时提供 `src/invariant.ts` 并在导出处调用，用于运行期断言服务契约。
- 抽象 seam 与具体实现拆分为独立 npm 包，通过 `peerDependencies` 声明依赖关系而非直接 import。
- 对外暴露的 service 以 `index.ts` 中的 `register*` / `create*` 函数注册到 Cordis 上下文（`ctx.*`），供其他能力消费。
- README 采用三语结构（README.md / README.zh.md / README.i18n.yaml），便于多语言文档生成。