- 所有外部配置对象先经 schemastery/zod schema 校验再 `deepFreeze` 为不可变结构，并通过 `assertPositiveInteger` 等守卫函数统一报错。
- 通过 `declare module '@deepseek-ai/dsh-session/types'` 在消费方扩展 `SessionEventMap`，新增的 `session/title`、`session/title-llm-request` 事件均以只读接口描述。
- LLM 插件以 `export const name` + `export const inject` + `export function apply(ctx, config)` 的固定三件套暴露，内部复用 `registerSessionTitleLlmProvider` 完成注册。
- 异步工作通过 revision 递增 + `AbortController` + `AbortSignal.any` 组合实现 supersede 语义，旧任务在 `assertCurrent` 中因 signal 或 state 变更被拒绝。
- 标题来源以联合类型 `SessionTitleSource = {kind:'fallback'|'provider'|'user'}` 表达，并在 `copySessionTitleSource` 中做防御性深拷贝以保证快照不可变。
- 对 provider 返回结果执行严格白名单校验（`validateResult`），要求 `title` 为非空字符串、`messageSeqs` 为有序唯一 seq 数组、`model` 可选且含非空 provider/model 字段。