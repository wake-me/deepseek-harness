- 每个子包独立 package.json 并通过 Cordis 插件机制向外暴露能力，而非被直接 import。
- 每个子包 src/invariant.ts 集中存放运行时不变量断言，业务代码不内联 assert。
- 持久化/投影/遥测等抽象层与其具体实现分离为相邻子包（如 persistence ↔ jsonl/sqlite，telemetry ↔ otel，title ↔ llm providers）。
- 测试目录与 src 一一对应，并按 .spec.ts / .e2e.ts 区分单元与端到端用例。