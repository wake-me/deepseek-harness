每个子包均为独立 npm 包（含 `package.json`、`tsconfig.json`），需分别 `npm install` 构建；`spill-local` 默认将 spill 文件写入 OS temp 下私有目录（`mkdtempSync(join(tmpdir(), 'dsh-spill-'))`，权限 0700），可通过 `Config.root` 覆盖；`spill-policy` 仅在配置 `maxInlineBytes` 为非负整数时注册，否则为 no-op。