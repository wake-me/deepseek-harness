pnpm install 后自动安装 lefthook 钩子；根命令统一入口包括：build/build:lib/build:web、test/test:e2e/test:snapshot/test:web*、check:all/check:ci/*、hygiene（vendor rescope/knip/publint/constraints/license/invariant 全量校验）、release:*（bump/pack/publish）、dsh（直接运行 CLI）、docs:dev/docs:build、mock:llm（LLM 模拟服务）。